
  # Landing and Reservation Pages

  This is a code bundle for Landing and Reservation Pages. The original project is available at https://www.figma.com/design/UtVmESwkFbDtbIMGix4VyW/Landing-and-Reservation-Pages.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  